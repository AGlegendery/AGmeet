# @ybouane/liquidglass declares `postinstall: patch-package` but does not ship
# patch-package (it is a devDependency, and the published tarball contains only
# dist/ and src/), so a plain `npm install` fails on it. Nothing else here
# needs an install script, and not running arbitrary code on install is the
# better default anyway.
ignore-scripts=true
